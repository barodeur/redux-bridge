import middleware from './middleware';
import { REDUX_BRIDGE_SYNC_INITIAL_STATE } from '../common/actions';
import regiserWebComponent from './registerWebComponent';

export {
  middleware,
  REDUX_BRIDGE_SYNC_INITIAL_STATE,
};
